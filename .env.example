# Where /api/* is proxied to in development. Leave unset in production, where
# the sign-in API is served from the same origin as the app.
# ALLYA_API_ORIGIN=https://ask.zeroto10.xyz

# The investor room's content API (backend/). Defaults to http://localhost:8010.
NEXT_PUBLIC_INVESTOR_API_URL=http://localhost:8010
